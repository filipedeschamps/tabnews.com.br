{
  "dependencies": {
    "cross-fetch": "3.1.4",
    "next": "11.1.0",
    "node-pg-migrate": "5.10.0",
    "pg": "8.6.0",
    "react": "17.0.2",
    "react-confetti": "6.0.1",
    "react-dom": "17.0.2",
    "react-icons": "4.2.0"
  },
  "devDependencies": {
    "@babel/preset-env": "7.14.7",
    "async-retry": "1.3.1",
    "autoprefixer": "10.3.1",
    "concurrently": "6.2.0",
    "cz-conventional-changelog": "3.3.0",
    "dotenv": "10.0.0",
    "eslint": "7.31.0",
    "eslint-config-next": "11.0.1",
    "eslint-config-prettier": "8.3.0",
    "jest": "27.0.6",
    "postcss": "8.3.5",
    "prettier": "2.3.2",
    "tailwindcss": "2.2.4"
  },
  "scripts": {
    "dev": "npm run services:up && npm run next && npm run services:stop",
    "next": "next dev",
    "services:up": "docker-compose -f infra/docker-compose.development.yml up -d",
    "services:stop": "docker-compose -f infra/docker-compose.development.yml stop",
    "build": "next build",
    "start": "next start",
    "test": "npm run services:up && concurrently -s -k -n next,jest 'npm run next' 'jest --runInBand' && npm run services:stop",
    "test:watch": "npm run services:up && concurrently -s -k -n next,jest 'npm run next' 'jest --runInBand --watch' && npm run services:stop",
    "test:ci": "jest --runInBand",
    "lint": "npm run lint:next && npm run lint:prettier",
    "lint:next": "next lint --dir .",
    "lint:prettier": "prettier --check '**/*.{js,jsx,ts,tsx}'",
    "commit": "cz"
  },
  "name": "tabnews.com.br",
  "description": "Conteúdos para quem vive de programação e tecnologia.",
  "version": "1.0.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/filipedeschamps/tabnews.com.br.git"
  },
  "keywords": [
    "news"
  ],
  "author": "Filipe Deschamps",
  "license": "GPL-3.0-only",
  "bugs": {
    "url": "https://github.com/filipedeschamps/tabnews.com.br/issues"
  },
  "homepage": "https://www.tabnews.com.br/",
  "jest": {
    "moduleDirectories": [
      "node_modules",
      "."
    ],
    "modulePathIgnorePatterns": [
      "<rootDir>/.next"
    ],
    "testTimeout": 120000,
    "slowTestThreshold": 40000,
    "setupFiles": [
      "dotenv/config"
    ]
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
